# commit to be tagged for the new release
commit = "HEAD"

project_name = "Omni Proxmox Infra Provider"
github_repo = "siderolabs/omni-infra-provider-proxmox"
match_deps = "^github.com/((talos-systems|siderolabs)/[a-zA-Z0-9-]+)$"
